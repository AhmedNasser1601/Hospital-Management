@model Doctor

<div class="row">
    <div class="col-md-8 offset-2">
        @if (User.IsInRole("Admin"))
        {
            <div class="form-group">
                <a class="btn btn-outline-secondary" asp-controller="Doctor" asp-action="Create">Add Doctor</a>
            </div>
        }

        <div class="row">
            <div class="col-md-8 offset-2">
                <table class="table">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Position</th>
                            @if (!User.IsInRole("Admin"))
                            {
                                <th>User</th>
                            }
                            else if (User.IsInRole("Admin"))
                            {
                                <th>Admin</th>
                            }
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td></td>
                            <td>@Model.Name</td>
                            <td>@Model.Position</td>
                            @if (!User.IsInRole("Admin"))
                            {
                                <td>
                                    <a class="alert-primary" href="/Appointment/Create/@Model.Id">Book Appointment</a>
                                </td>
                            }
                            else if (User.IsInRole("Admin"))
                            {
                                <td>
                                    <a class="alert-primary" href="/Doctor/Edit/@Model.Id">Edit</a>
                                    <a class="alert-danger" href="/Doctor/Delete/@Model.Id">Delete</a>
                                </td>
                            }
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
